# `guardian/actions-publish-image`

A ([composite](https://docs.github.com/en/actions/tutorials/create-actions/create-a-composite-action)) GitHub Action to tag and push a Docker image to Amazon ECR.

See [`action.yml`](action.yml) for details on the available inputs and outputs.

## Example usage

```yaml

```